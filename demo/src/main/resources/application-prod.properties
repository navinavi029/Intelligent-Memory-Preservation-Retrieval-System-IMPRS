# Production Profile

# JPA Configuration - Validate schema only in production
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Production database from environment
spring.datasource.url=${DATABASE_URL}
